---
title: "slint::WindowKind Enum"
description: "The window kind when creating a new child window."
---
```cpp
enum class WindowKind
```

| Value | Description |
| --- | --- |
| `ToolTip` | Tooltip. |
| `Popup` | Popup [Window](../window/). |
| `Menu` | Popup Menu. |

The window kind when creating a new child window.