---
title: "slint::interpreter::DiagnosticLevel Enum"
description: "DiagnosticLevel describes the severity of a diagnostic."
---
```cpp
enum class DiagnosticLevel
```

| Value | Description |
| --- | --- |
| `Error` | The diagnostic belongs to an error. |
| `Warning` | The diagnostic belongs to a warning. |
| `Note` | The diagnostic is a note. |

DiagnosticLevel describes the severity of a diagnostic.