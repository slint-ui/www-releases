---
title: "slint::platform::duration_until_next_timer_update Function"
description: "Returns the duration until the next timer if there are pending timers."
---
```cpp
std::optional<std::chrono::milliseconds> slint::platform::duration_until_next_timer_update()
```

Returns the duration until the next timer if there are pending timers.