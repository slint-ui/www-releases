---
title: "Color"
---
import XRef from "../../../../components/XRef.astro";
import Signature from "../../../../components/Signature.astro";

```python
from slint import Color
```

## Properties

### red

<Signature symbol="slint.slint.Color.red">red: <XRef to="int" plain /></Signature>

### green

<Signature symbol="slint.slint.Color.green">green: <XRef to="int" plain /></Signature>

### blue

<Signature symbol="slint.slint.Color.blue">blue: <XRef to="int" plain /></Signature>

### alpha

<Signature symbol="slint.slint.Color.alpha">alpha: <XRef to="int" plain /></Signature>

## Methods

### brighter

<Signature symbol="slint.slint.Color.brighter">brighter(factor: <XRef to="float" plain />) -&gt; <XRef to="Color" plain /></Signature>

### darker

<Signature symbol="slint.slint.Color.darker">darker(factor: <XRef to="float" plain />) -&gt; <XRef to="Color" plain /></Signature>

### transparentize

<Signature symbol="slint.slint.Color.transparentize">transparentize(factor: <XRef to="float" plain />) -&gt; <XRef to="Color" plain /></Signature>

### mix

<Signature symbol="slint.slint.Color.mix">mix(other: <XRef to="Image" plain />, factor: <XRef to="float" plain />) -&gt; <XRef to="Color" plain /></Signature>

### with_alpha

<Signature symbol="slint.slint.Color.with_alpha">with_alpha(alpha: <XRef to="float" plain />) -&gt; <XRef to="Color" plain /></Signature>